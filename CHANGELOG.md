# 更新日志

## [2.2.1] - 2026-09-22

### 新增
- `max_tokens` 与最大响应时间均增加「无上限」选项；无上限时不向请求施加对应限制。
- 根据服务端 `usage` 累计 Token，每超过一千万 Token 弹窗提醒一次；连续 300 秒未收到响应数据时弹出耗时提醒，但不中断无上限请求。

### 修复
- 模型列表仅在 Base URL 与 API Key 完整时允许查询，并直接使用设置窗口中尚未保存的地址与 Key。
- 将 401、403、404、429 和网络错误压缩为短提示，避免服务端长错误撑坏设置窗口。
- 设置表单改为窄窗口友好的纵向布局，正文独立滚动，底部「取消 / 保存设置」始终可见。

### 变更
- 设置窗口移除温度选项，工作台请求默认交由模型服务决定采样温度。

## [2.2.0] - 2026-09-19

### 新增
- 右侧对话改为消息卡片，支持 Markdown 标题、列表、引用、强调及代码块。
- 模型返回的思考内容可流式显示、展开/收起，并限制高度以适应窄窗口。
- 增加等待/生成计时、呼吸指示、逐字显示、淡入和折叠动画，以及「动效」开关和「回到最新回复」。

### 修复
- 非流式 Chat/Builder 也展示模型返回的思考内容，流式完成时不重复追加。
- 取消请求后忽略迟到的内容和完成信号，避免状态闪成错误或污染下一轮消息。

### 测试
- 新增 Markdown 安全转义、流式不完整格式、思考内容传递、窄栏布局、折叠和动画生命周期离线测试。

## [2.1.2] - 2026-09-17

### 修复
- 设置窗口的输入框、数字框和复选框统一使用工作台字号，修正 Mac 上文字和控件被重复放大的问题。
- 缩小设置窗口默认尺寸；表单可滚动，取消和保存按钮固定在底部，避免内容被截断。

### 变更
- 根据职责重命名插件、示例、测试和图标文件，并同步更新所有项目内引用及资源 UID。
- 新增 `FILE_PURPOSES.md`，逐一说明仓库文件的用途及旧文件名对应关系。直接加载旧路径的外部项目需更新引用。

### 测试
- Godot 4.6.1 冒烟测试和本地 HTTP 集成测试通过。

## [2.1.1] - 2026-09-17

### 修复
- 悬浮工作台和设置窗口跟随 Godot 编辑器缩放，旧窗口尺寸按比例迁移并限制在可用屏幕内。
- 结果区默认显示逐行 Diff，完整代码左右同步滚动，手动调整列宽不会被布局刷新重置。
- Builder 至少等待 180 秒；服务端因输出长度上限截断时明确报错，流式请求收到整包 JSON 也能解析。
- 回滚后刷新编辑器资源索引；场景仍待恢复时保留事务记录，先恢复磁盘脚本再恢复编辑器中的脚本和节点。

## [2.1.0] - 2026-09-16

### 新增
- **悬浮 AI 工作台**：独立窗口提供 GitHub Primer 深色 / Trae 风格三栏界面，计划、结果和对话不再挤在同一个 Dock，也不再占满顶部主屏。
- **统一 Builder 状态机**：`idle → planning → generating → review → applying → done/error/cancelled`，Chat 保持只读问答。
- **多文件 Builder 契约**：一次响应可包含多个 `.gd` create/update 和选中节点的 `attach_script` 操作。
- **独立结果页**：展示受影响文件、完整 Before/After、统一 Diff、节点绑定卡和唯一「应用全部」入口。
- **事务应用与回滚**：任务开始时锁定场景、节点和脚本版本；应用任一步失败时恢复已写文件、已打开脚本、节点脚本/导出属性与场景快照。
- **崩溃恢复**：事务开始前持久化恢复记录；插件重载后继续阻止新任务并提供「Retry Rollback」，直到未完成事务被完整恢复。
- **精确编辑**：已有脚本可使用唯一 `old_string/new_string` 生成内存草稿；审查前不写磁盘。

### 变更
- 退役旧聊天 Dock、AI 任务 Dock、逐提案应用和脚本顶部 Keep/Undo，避免并存多套修改工作流。
- 版本控制继续作为独立底部面板，并统一到工作台深色视觉。
- 文件写入改为临时文件、校验备份和原子重命名；全部文件落盘后统一校验多文件 GDScript 依赖，失败即回滚。

### 修复
- 工作台改为可拖动的悬浮窗口（默认约 1280×720），不再占用顶部主屏；工具栏与「项目 → 工具」均可打开。
- 设置窗口改为挂到编辑器根节点并铺满内容区，避免嵌在工作台内时出现空白窗。
- 工作台分栏改为接近 Godot 编辑器：左 Dock 约 260、右 Inspector 约 360、中间视口更深；顶栏约 36px；分栏条可见。
- 工作台分栏改为响应式比例；窄窗口自动收起任务栏并支持手动展开，文件列表与代码对比区也会随可用宽度调整。

### 测试
- 冒烟测试新增 Builder 多文件、路径/符号链接沙箱、节点操作校验、精确编辑、原子失败回滚、场景与节点恢复、多文件依赖、跨重载事务恢复、审查前不落盘和工作台构建覆盖。
- Godot 4.6 编辑器加载与本地 OpenAI 兼容 HTTP mock 集成测试通过。

## [2.0.1] - 2026-09-03

### 修复
- 任务面板与版本控制面板读取编辑器设置时先检查键是否存在，避免 Godot 4.6 中 `get_setting` 不接受默认参数导致报错。
- 底部面板实例化时显式转换为 `Control`，加载失败时给出明确错误而不是类型不匹配。

## [2.0.0] - 2026-08-26

### 新增
- **AI 任务面板**：把自然语言需求和选中场景节点的上下文交给模型，先返回计划与 JSON 文件提案，再由用户逐项应用或跳过。
- **提案安全链路**：仅允许 `res://` 下的 `.gd` 文件；模型不能直接落盘；删除必须点「确认删除」；每次应用前自动存会话快照，可一键回滚本轮已应用改动。
- **节点脚本提案**：从项目菜单打开，可读取当前场景节点类型、父子关系、信号和已有脚本；用户可选择在应用后挂载生成脚本，场景保存仍由用户确认。
- **版本控制面板**：底部面板提供工作区状态、差异预览、暂存、取消暂存、丢弃改动、提交与最近历史。
- **AI 提交信息**：只读取已暂存 diff 生成 Conventional Commit 文本，用户审阅后自行点击提交。
- **会话内 API 配置共享**：未勾选「记住 API Key」时，任务和版本控制面板仍可使用本次编辑器会话的 Key，不写入磁盘。

### 改进
- 思考内容默认收起；工具栏可展开查看，避免与最终回答混排。
- 聊天输入旁增加停止按钮，可中止正在进行的请求。

### 测试
- 冒烟测试新增提案 JSON 解析和路径沙箱覆盖。

本项目遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [1.1.5] - 2026-08-25

### 修复
- 修正「发送」按钮的启用/禁用逻辑：首次打开面板即可点击发送（此前逻辑写反，按钮保持灰显、只能回车）。
- 流式输出改为按字节缓存、跨网络包续接后再解码，修复中文等 UTF-8 多字节字符在分包边界被拆坏导致的乱码。
- SSE 解析兼容 `\r\n\r\n` 分隔，并在连接结束时 flush 残留的最后一个数据块（部分服务端不补结尾空行导致丢尾）。
- 把 `json.data` 强制转为字典前增加类型校验，避免畸形/非对象响应触发运行时崩溃。
- 运行时从环境变量读取 Key / Base URL / 模型时去除首尾空白，避免 `.env` 尾随换行引发 401。

## [1.1.4] - 2026-08-24

### 里程碑
- **首个公开发布版本**：README 由作者重写并整理，仓库发布至 GitHub。
- 版本号规范化（1.0.8 → 1.1.0），作为后续公开迭代的基线。

## [1.0.8] - 2026-08-23

### 新增
- **「我的模型」收藏**：API 返回的全部模型不再直接堆进面板下拉框；在设置窗口「服务器模型」里选中后用「＋加入」收藏，「－移除」可删，收藏持久化。
- **设置窗口「刷新」直接使用输入框里的 Key**：无需先点保存即可拉取模型列表，避免"空 Key 请求 → 401"的困惑。
- 模型缓存按 `Base URL + Key 哈希指纹` 存储，重启编辑器不重复请求。

### 变更
- Base URL / 模型**首次打开留空**（占位符提示），不再预写默认地址，避免"凭空出现一个没填过的地址"的疑惑。
- 未勾选「记住 API Key」时，启动顺带清除历史遗留的已存 Key。
- 「记住 API Key」勾选框文案简化，说明移至悬停提示。

### 修复
- 模型列表请求失败时显示**服务商返回的具体错误原因**（如 401 的原始 message），便于排查。
- Authorization 头使用去除首尾空格后的 Key。

## [1.0.7] - 2026-08-23

- 模型列表请求可用「Key 覆盖」：设置窗口里输入框未保存的 Key 也能用于拉取模型。
- 未填 Key 时明确提示，不再静默发无鉴权请求。
- 模型缓存指纹与"实际使用的 Key"对齐。

## [1.0.6] - 2026-08-23

- 模型列表接口非 200 时，解析并显示服务商返回的具体错误信息。
- `fetch_models()` 的 Authorization 头去除 Key 首尾空格。

## [1.0.5] - 2026-08-23

- **保存设置后自动拉取可用模型**，并自动选中第一个可用模型（若当前模型为空）。
- 按 `Base URL + Key 指纹` 缓存模型列表，重启不重复请求。
- 快捷模型列表新增通义千问系列（qwen-turbo/plus/max/long）。

## [1.0.4] - 2026-08-23

- 面板工具栏新增「刷新模型」按钮，模型下拉动态加载服务器可用模型。

## [1.0.3] - 2026-08-23

- 新增 `fetch_models()` / `models_loaded` 信号：从 `GET {base}/models` 拉取当前 Key 可用模型。
- 抽出 `_parse_base_url()` 统一 URL 解析；新增 `build_models_url()`。

## [1.0.2] - 2026-08-22

### 修复
- **HTTP 404 根因**：base_url 带路径前缀（如 `/v1`）时请求地址拼接错误；现在正确拼接 `{base}/chat/completions`，且容错误填完整端点的情况。
- 请求报错信息附带**实际请求的完整 URL**，便于排查。
- 地址中带 query（如 mock 测试参数）时正确拆分。

## [1.0.1] - 2026-08-22

### 修复
- **API Key 不再默认持久化**：未勾选「记住 API Key」时仅存内存，启动时顺带清除历史遗留 Key；设置里新增「清除已保存的 Key」按钮。
- **设置窗口大小问题**：Godot 4.6 中 `Window.resizable` 属性不存在导致窗口尺寸错误；改用 `unresizable` 探测，窗口可正常缩放，设置窗口与系统提示词窗口尺寸/最小尺寸均已适配。

## [1.0.0] - 2026-08-21

- 首个可用版本：编辑器内 AI 聊天面板 + 游戏运行时 `AI` 全局单例。
- 支持任意 OpenAI 兼容后端（DeepSeek / OpenAI / Ollama / vLLM 等）。
- 流式输出（SSE）、`deepseek-reasoner` 思考过程展示、请求队列、历史维护。
- 「发送当前脚本」分析、回复代码块插入/另存、运行场景快捷入口。
